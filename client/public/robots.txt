# Tymio — default crawl policy for the main web app (static file served from site root).
# Adjust per deployment if you run on a private host.

User-agent: *
Allow: /

# Sitemap: update the hostname if you self-host (must match public origin).
Sitemap: https://tymio.app/sitemap.xml

# Machine-readable site summary (UI languages, MCP, APIs): /llms.txt (see https://llmstxt.org/)
