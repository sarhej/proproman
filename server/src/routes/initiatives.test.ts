import { describe, it, expect } from "vitest";

describe("initiatives archive", () => {
  it("list excludes archived by default", () => {
    expect(true).toBe(true);
  });
  it("archive and unarchive endpoints exist", () => {
    expect(true).toBe(true);
  });
});

describe("success criteria", () => {
  it("GET/POST/PATCH/DELETE success criteria by initiative", () => {
    expect(true).toBe(true);
  });
});
