import { describe, it, expect } from "vitest";

describe("messages", () => {
  it("GET /api/messages returns messages and unreadCount", () => {
    expect(true).toBe(true);
  });
  it("PATCH /api/messages/:id/read sets readAt", () => {
    expect(true).toBe(true);
  });
});
