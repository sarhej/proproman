export const ATLAS_CURATOR_AGENT_ID = "atlas-curator";

/** Cap topics per run to control LLM cost and latency. */
export const ATLAS_CURATOR_MAX_TOPICS_PER_RUN = 10;

export const ATLAS_CURATOR_GIT_ACTIVITY_LIMIT = 15;
