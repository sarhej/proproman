NODE_ENV=development
PORT=8080
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/dd_product_board
SESSION_SECRET=dev-session-secret-change-me-12345
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=
# Optional: Microsoft Entra / personal Microsoft ("Sign in with Microsoft") — redirect URI e.g. http://localhost:8080/api/auth/microsoft/callback
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
MICROSOFT_CALLBACK_URL=
# Magic link email (optional): Resend (https://resend.com) + public API origin for links
# RESEND_API_KEY=
# RESEND_FROM=onboarding@resend.dev
# API_PUBLIC_URL=http://127.0.0.1:8080
# EMAIL_MAGIC_LINK_TTL_MINUTES=30
# Workspace registration / approval emails (E1–E4): must be explicit true or sends stay off
# TRANSACTIONAL_EMAIL_ENABLED=true
# Optional: provision workspace immediately on POST /api/tenant-requests (no SUPER_ADMIN review).
# Requires working DB migrations / provisionTenant. Same approval email (E2) as manual approve when Resend is configured.
# AUTO_APPROVE_WORKSPACE_REQUESTS=true
CLIENT_URL=http://localhost:5173
ALLOW_DEV_AUTH=true
DEV_AUTH_EMAIL=local-admin@tymio.local
DEV_AUTH_NAME=Local Admin
DEV_AUTH_ROLE=ADMIN
